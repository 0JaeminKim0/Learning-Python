### Zip and Argument Unpacking

# zip : two or more lists together
#		Transform multiple lists into a single list of
#		tuples of corresponding elements:

list1 = ['a', 'b', 'c']
list2 = [1, 2, 3]
print(zip(list1, list2))

# result : [('a', 1), ('b', 2), ('c', 3)]


# Unzip
pairs = [('a', 1), ('b', 2), ('c', 3)]
letters, numbers = zip(*pairs)
print(letters, numbers)
